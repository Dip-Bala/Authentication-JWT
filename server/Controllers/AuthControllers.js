export const register = async (req, res, next) => {
    res.json({ message: "Register function executed!" });
};

export const login = async (req, res, next) => {
    res.json({ message: "Login function executed!" });
};
